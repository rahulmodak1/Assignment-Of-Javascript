 // Let "A" be a string. Remove all the whitespaces and find it's length :-

// Answer :-

let A = "We are Learner of Board Infinity"
var result = A.replace(/\s/g , '') ;
console.log(result) ;
console.log(result.length) ;